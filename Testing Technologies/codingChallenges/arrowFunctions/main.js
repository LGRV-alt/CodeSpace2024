// Challenge 1 - Greet
let greet = (str) => `Hello ${str}!`;

// Challenge 2 - isEven
let isEven = (num) => num % 2 === 0;

// Challenge 3 - Counter Function
let counterFunction = (counter) => {
  counter > 100 ? (counter = 0) : counter++;
  return counter;
};

// Challenge 4 - Name and Age
let nameAge = (name, age) => {
  console.log(`Hello ${name}`);
  console.log(`You are ${age} years old`);
};

// Challenge 5 - Print
let printOnly = () => console.log("printing");

// Challenge 6 - Sum
let sum = (num1, num2) => num1 + num2;
